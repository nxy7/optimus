config:
  shell: bash

start: |
  echo test
  x=2
  y=3
  z=x+y
  echo z
  export testVar=testVal

e2e_tests: |
  ${startup}

services:
  frontend:
    start:
    dev:
    test:
      depends_on:
      - backend/main
      - backend/stream_chat

  backend/main:
  backend/stream_chat:

cleanup: |

