global:
  shell_cmd: bash -c

init: |
  echo test
  x=2
  y=3
  z=x+y
  echo z
  false
  echo $?
  false

e2e_tests: |
  ${startup}
  ./e2e pnpm test
  ${cleanup}

services:
  frontend:
    start:
    dev:
    test:
      depends_on:
      - backend/main
      - backend/stream_chat

  backend/main:
  backend/stream_chat:

purge: |

